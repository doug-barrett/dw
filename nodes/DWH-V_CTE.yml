steps:
  V_CTE-6b80cf89-b840-4dbc-a908-fca83708e6ec:
    operation:
      config:
        insertStrategy: UNION
        selectDistinct: false
      database: ""
      deployEnabled: true
      description: ""
      isMultisource: false
      locationName: DWH
      materializationType: view
      metadata:
        appliedNodeTests: []
        columns:
          - acceptedValues:
              strictMatch: true
              values: []
            appliedColumnTests: {}
            columnReference:
              columnCounter: 9f13a589-0a2c-48fb-83d7-f399b464e767
              stepCounter: 6b80cf89-b840-4dbc-a908-fca83708e6ec
            config: {}
            dataType: NUMBER(38,0)
            defaultValue: ""
            description: ""
            name: SUPPKEY
            nullable: false
            sourceColumnReferences:
              - columnReferences: []
                transform: ""
            transform: ""
          - acceptedValues:
              strictMatch: true
              values: []
            appliedColumnTests: {}
            columnReference:
              columnCounter: eee0ff41-7990-4e5f-8e78-cd456d31d1e2
              stepCounter: 6b80cf89-b840-4dbc-a908-fca83708e6ec
            config: {}
            dataType: VARCHAR(53)
            defaultValue: ""
            description: ""
            name: SUPP_NAME
            nullable: false
            sourceColumnReferences:
              - columnReferences: []
                transform: ""
            transform: ""
          - acceptedValues:
              strictMatch: true
              values: []
            appliedColumnTests: {}
            columnReference:
              columnCounter: 708ac33d-1586-483c-81fa-212c526a6856
              stepCounter: 6b80cf89-b840-4dbc-a908-fca83708e6ec
            config: {}
            dataType: VARCHAR(25)
            defaultValue: ""
            description: ""
            name: NATION
            nullable: false
            sourceColumnReferences:
              - columnReferences: []
                transform: ""
            transform: ""
          - acceptedValues:
              strictMatch: true
              values: []
            appliedColumnTests: {}
            columnReference:
              columnCounter: e7dfcebc-6370-48a5-be3d-cbb5853bd540
              stepCounter: 6b80cf89-b840-4dbc-a908-fca83708e6ec
            config: {}
            dataType: NUMBER(38,6)
            defaultValue: ""
            description: ""
            name: TOTAL_SALES
            nullable: true
            sourceColumnReferences:
              - columnReferences: []
                transform: ""
            transform: ""
          - acceptedValues:
              strictMatch: true
              values: []
            appliedColumnTests: {}
            columnReference:
              columnCounter: b325d6e4-5357-457d-9afb-e580eaa22737
              stepCounter: 6b80cf89-b840-4dbc-a908-fca83708e6ec
            config: {}
            dataType: NUMBER(38,6)
            defaultValue: ""
            description: ""
            name: TOP_SALES
            nullable: true
            sourceColumnReferences:
              - columnReferences: []
                transform: ""
            transform: ""
          - acceptedValues:
              strictMatch: true
              values: []
            appliedColumnTests: {}
            columnReference:
              columnCounter: 84e8f677-2f97-44c6-a0e8-cb22b2074b20
              stepCounter: 6b80cf89-b840-4dbc-a908-fca83708e6ec
            config: {}
            dataType: NUMBER(38,6)
            defaultValue: ""
            description: ""
            name: MID_SALES
            nullable: true
            sourceColumnReferences:
              - columnReferences: []
                transform: ""
            transform: ""
          - acceptedValues:
              strictMatch: true
              values: []
            appliedColumnTests: {}
            columnReference:
              columnCounter: 77a7bb19-591f-4b95-a187-ff486903fbbb
              stepCounter: 6b80cf89-b840-4dbc-a908-fca83708e6ec
            config: {}
            dataType: NUMBER(38,6)
            defaultValue: ""
            description: ""
            name: BOTTOM_SALES
            nullable: true
            sourceColumnReferences:
              - columnReferences: []
                transform: ""
            transform: ""
          - acceptedValues:
              strictMatch: true
              values: []
            appliedColumnTests: {}
            columnReference:
              columnCounter: d15708ab-411e-485b-a8a5-f638417b2f49
              stepCounter: 6b80cf89-b840-4dbc-a908-fca83708e6ec
            config: {}
            dataType: VARCHAR(16777216)
            defaultValue: ""
            description: ""
            name: TOP_NATIONS
            nullable: false
            sourceColumnReferences:
              - columnReferences: []
                transform: ""
            transform: ""
        cteString: ""
        enabledColumnTestIDs: []
        sourceMapping:
          - aliases: {}
            customSQL:
              customSQL: |-
                {{ stage('Override Create SQL') }}
                	CREATE OR REPLACE VIEW {{ ref('DWH', 'V_CTE')}} AS (
                WITH 
                    sales_year as (
                        SELECT TOP 5 YEAR(O_ORDERDATE) YR
                        FROM {{ref('SRC_SAMPLE','ORDERS')}}
                        GROUP BY YEAR(O_ORDERDATE)
                        ORDER BY YEAR(O_ORDERDATE) DESC
                        )
                    ,sales_base AS (
                        SELECT L.L_SUPPKEY SUPPKEY, N.N_NAME NATION, 
                               SUM(((L.L_EXTENDEDPRICE * L.L_QUANTITY) * (1 - L.L_DISCOUNT))) SALES_AMT
                        FROM {{ref('SRC_SAMPLE','LINEITEM')}} L
                        JOIN {{ref('SRC_SAMPLE','ORDERS')}} O ON L.L_ORDERKEY = O.O_ORDERKEY
                        JOIN {{ref('SRC_SAMPLE','CUSTOMER')}} C ON O.O_CUSTKEY = C.C_CUSTKEY
                        JOIN {{ref('SRC_SAMPLE','NATION')}} N ON C.C_NATIONKEY = N.N_NATIONKEY
                        --LEFT JOIN DOUG_DB.DEV_STG.EXCLUDE_SUPPLIERS E ON L.L_SUPPKEY = E.E_SUPPKEY
                        WHERE --E.E_SUPPKEY IS NULL AND
                          O.O_ORDERSTATUS = 'F'
                         AND YEAR(O.O_ORDERDATE) in (SELECT YR FROM sales_year)
                        GROUP BY SUPPKEY, NATION)
                    ,supplier_sales as (
                        SELECT SUPPKEY, SUM(SALES_AMT) SALES_AMT
                        FROM sales_base
                        GROUP BY SUPPKEY)
                    ,nation_rank as (
                        SELECT SUPPKEY, NATION, SALES_AMT,
                               ROW_NUMBER() OVER (PARTITION BY SUPPKEY ORDER BY SALES_AMT) NATION_RANK,
                               ROW_NUMBER() OVER (PARTITION BY SUPPKEY ORDER BY SALES_AMT DESC) NATION_RANK_REV
                        FROM sales_base)
                    ,top_nation_sales as (
                        SELECT SUPPKEY, SUM(SALES_AMT) SALES_AMT, LISTAGG(NATION, ',') WITHIN GROUP (ORDER BY NATION_RANK) TOP_NATIONS
                        FROM nation_rank
                        WHERE NATION_RANK <= 10
                        GROUP BY SUPPKEY)
                    ,mid_nation_sales as (
                        SELECT SUPPKEY, SUM(SALES_AMT) SALES_AMT, LISTAGG(NATION, ',') WITHIN GROUP (ORDER BY NATION_RANK) MID_NATIONS
                        FROM nation_rank
                        WHERE NATION_RANK > 10 AND NATION_RANK_REV > 10
                        GROUP BY SUPPKEY)
                    ,bottom_nation_sales as (
                        SELECT SUPPKEY, SUM(SALES_AMT) SALES_AMT, LISTAGG(NATION, ',') WITHIN GROUP (ORDER BY NATION_RANK) BOTTOM_NATIONS
                        FROM nation_rank
                        WHERE NATION_RANK_REV <= 10
                        GROUP BY SUPPKEY)
                    ,supplier_details as (
                        SELECT S_SUPPKEY SUPPKEY, S_NAME || ' ('||N_NAME||')' SUPP_NAME, N_NAME NATION
                        FROM {{ref('SRC_SAMPLE','SUPPLIER')}} S
                        JOIN {{ref('SRC_SAMPLE','NATION')}} N ON S.S_NATIONKEY = N.N_NATIONKEY)
                SELECT s.SUPPKEY, s.SUPP_NAME, s.NATION, 
                       total.SALES_AMT TOTAL_SALES, 
                       top.SALES_AMT TOP_SALES,
                       mid.SALES_AMT MID_SALES,
                       bot.SALES_AMT BOTTOM_SALES,
                       top.TOP_NATIONS 
                FROM supplier_details s 
                JOIN supplier_sales total ON s.SUPPKEY = total.SUPPKEY
                JOIN top_nation_sales top ON  s.SUPPKEY = top.SUPPKEY
                JOIN mid_nation_sales mid ON  s.SUPPKEY = mid.SUPPKEY
                JOIN bottom_nation_sales bot ON  s.SUPPKEY = bot.SUPPKEY
                ORDER BY 1
                	)
            dependencies:
              - locationName: SRC_SAMPLE
                nodeName: CUSTOMER
              - locationName: SRC_SAMPLE
                nodeName: LINEITEM
              - locationName: SRC_SAMPLE
                nodeName: NATION
              - locationName: SRC_SAMPLE
                nodeName: ORDERS
              - locationName: SRC_SAMPLE
                nodeName: SUPPLIER
            join:
              joinCondition: |-
                WITH 
                    sales_year as (
                        SELECT TOP 5 YEAR(O_ORDERDATE) YR
                        FROM {{ref('SRC_SAMPLE','ORDERS')}}
                        GROUP BY YEAR(O_ORDERDATE)
                        ORDER BY YEAR(O_ORDERDATE) DESC
                        )
                    ,sales_base AS (
                        SELECT L.L_SUPPKEY SUPPKEY, N.N_NAME NATION, 
                               SUM(((L.L_EXTENDEDPRICE * L.L_QUANTITY) * (1 - L.L_DISCOUNT))) SALES_AMT
                        FROM {{ref('SRC_SAMPLE','LINEITEM')}} L
                        JOIN {{ref('SRC_SAMPLE','ORDERS')}} O ON L.L_ORDERKEY = O.O_ORDERKEY
                        JOIN {{ref('SRC_SAMPLE','CUSTOMER')}} C ON O.O_CUSTKEY = C.C_CUSTKEY
                        JOIN {{ref('SRC_SAMPLE','NATION')}} N ON C.C_NATIONKEY = N.N_NATIONKEY
                        --LEFT JOIN DOUG_DB.DEV_STG.EXCLUDE_SUPPLIERS E ON L.L_SUPPKEY = E.E_SUPPKEY
                        WHERE --E.E_SUPPKEY IS NULL AND
                          O.O_ORDERSTATUS = 'F'
                         AND YEAR(O.O_ORDERDATE) in (SELECT YR FROM sales_year)
                        GROUP BY SUPPKEY, NATION)
                    ,supplier_sales as (
                        SELECT SUPPKEY, SUM(SALES_AMT) SALES_AMT
                        FROM sales_base
                        GROUP BY SUPPKEY)
                    ,nation_rank as (
                        SELECT SUPPKEY, NATION, SALES_AMT,
                               ROW_NUMBER() OVER (PARTITION BY SUPPKEY ORDER BY SALES_AMT) NATION_RANK,
                               ROW_NUMBER() OVER (PARTITION BY SUPPKEY ORDER BY SALES_AMT DESC) NATION_RANK_REV
                        FROM sales_base)
                    ,top_nation_sales as (
                        SELECT SUPPKEY, SUM(SALES_AMT) SALES_AMT, LISTAGG(NATION, ',') WITHIN GROUP (ORDER BY NATION_RANK) TOP_NATIONS
                        FROM nation_rank
                        WHERE NATION_RANK <= 10
                        GROUP BY SUPPKEY)
                    ,mid_nation_sales as (
                        SELECT SUPPKEY, SUM(SALES_AMT) SALES_AMT, LISTAGG(NATION, ',') WITHIN GROUP (ORDER BY NATION_RANK) MID_NATIONS
                        FROM nation_rank
                        WHERE NATION_RANK > 10 AND NATION_RANK_REV > 10
                        GROUP BY SUPPKEY)
                    ,bottom_nation_sales as (
                        SELECT SUPPKEY, SUM(SALES_AMT) SALES_AMT, LISTAGG(NATION, ',') WITHIN GROUP (ORDER BY NATION_RANK) BOTTOM_NATIONS
                        FROM nation_rank
                        WHERE NATION_RANK_REV <= 10
                        GROUP BY SUPPKEY)
                    ,supplier_details as (
                        SELECT S_SUPPKEY SUPPKEY, S_NAME || ' ('||N_NAME||')' SUPP_NAME, N_NAME NATION
                        FROM {{ref('SRC_SAMPLE','SUPPLIER')}} S
                        JOIN {{ref('SRC_SAMPLE','NATION')}} N ON S.S_NATIONKEY = N.N_NATIONKEY)
                SELECT s.SUPPKEY, s.SUPP_NAME, s.NATION, 
                       total.SALES_AMT TOTAL_SALES, 
                       top.SALES_AMT TOP_SALES,
                       mid.SALES_AMT MID_SALES,
                       bot.SALES_AMT BOTTOM_SALES,
                       top.TOP_NATIONS 
                FROM supplier_details s 
                JOIN supplier_sales total ON s.SUPPKEY = total.SUPPKEY
                JOIN top_nation_sales top ON  s.SUPPKEY = top.SUPPKEY
                JOIN mid_nation_sales mid ON  s.SUPPKEY = mid.SUPPKEY
                JOIN bottom_nation_sales bot ON  s.SUPPKEY = bot.SUPPKEY
                ORDER BY 1
            name: V_CTE
            noLinkRefs: []
      name: V_CTE
      overrideSQL: true
      schema: ""
      sqlType: View
      type: sql
    stepCounter: 6b80cf89-b840-4dbc-a908-fca83708e6ec
