---
created: 2026-06-26T16:56:50.492Z
session: 978311bb-8dcd-4887-b769-17a83a5881c6
working_directory: /Users/dougy/GIT/dw
---

## Context

The SQL in `~/work/tmp/Trips.sql` is a multi-CTE query referencing 4 source tables from `NY_TAXI.BRONZE`. I'll decompose it into a full Coalesce pipeline using V1 (YAML) node format with the built-in Stage node type.

**Source tables:** VENDOR, VENDOR_DETAILS, YELLOW_CAB_TRIPS, LOCATION
**Transformation nodes:** 9 Stage nodes (one per CTE + final SELECT)

## Implementation Steps

1. **Create pipeline scaffold** in `~/work/tmp/Trips/`:
   - `data.yml` — root metadata (fileVersion: 1, platformKind: snowflake)
   - `locations.yml` — SRC, STG locations
   - `workspace.yml` — map SRC → NY_TAXI.BRONZE, STG → NY_TAXI.STAGING

2. **Create 4 Source nodes** (V1 YAML, `nodes/SRC-*.yml`):
   - SRC_VENDOR (VENDOR_ID, VENDOR_NAME)
   - SRC_VENDOR_DETAILS (VENDOR_ID, PHONE, DRIVERS, HQ_ADDRESS_DETAILS)
   - SRC_YELLOW_CAB_TRIPS (VENDOR_ID, PU_LOCATION_ID, PICKUP_DATETIME, TRIP_DISTANCE, FARE_AMOUNT)
   - SRC_LOCATION (LOCATION_ID, ZONE)

3. **Create 9 Stage nodes** (V1 YAML, `nodes/STG-*.yml`):
   - STG_VENDOR — simple passthrough from SRC_VENDOR
   - STG_VENDOR_DETAILS — JSON extraction from SRC_VENDOR_DETAILS
   - STG_VENDOR_CLEAN_PHONE — REGEXP_REPLACE on phone from STG_VENDOR_DETAILS
   - STG_FULL_VENDOR — JOIN of STG_VENDOR + STG_VENDOR_CLEAN_PHONE
   - STG_VENDOR_CLASSIFICATION — CASE expressions from STG_FULL_VENDOR
   - STG_VENDOR_ACTIVITY_7 — aggregation from SRC_YELLOW_CAB_TRIPS + SRC_LOCATION
   - STG_VENDOR_LOCATION_ACTIVITY_7 — aggregation with QUALIFY from SRC_YELLOW_CAB_TRIPS + SRC_LOCATION
   - STG_VENDOR_LOCATION_SUMMARY_7 — LISTAGG from STG_VENDOR_LOCATION_ACTIVITY_7
   - STG_TRIPS — final JOIN of classification + activity + location summary

4. **Validate** with `coa validate -d ~/work/tmp/Trips`

## Verification

- `coa validate -d ~/work/tmp/Trips` — passes schema + graph validation
- `coa create -d ~/work/tmp/Trips --dry-run --json` — DDL renders without errors
- `coa run -d ~/work/tmp/Trips --dry-run --json` — DML renders without errors

## Critical Files

- `~/work/tmp/Trips/data.yml` — Root pipeline metadata
- `~/work/tmp/Trips/locations.yml` — Location definitions
- `~/work/tmp/Trips/nodes/STG-STG_TRIPS.yml` — Final output node (most complex joins)
- `~/work/tmp/Trips/nodes/SRC-SRC_YELLOW_CAB_TRIPS.yml` — Source with most columns
- `~/work/tmp/Trips/nodes/STG-STG_VENDOR_ACTIVITY_7.yml` — Complex aggregation with window functions